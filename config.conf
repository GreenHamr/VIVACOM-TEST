# IP Inventory backend – конфигурация
# Променливите на средата (IPINVENTORY_*) презаписват стойностите от този файл.

host=127.0.0.1
port=8888

# --- База данни ---
# db_type: sqlite | postgresql | mssql | oracle
# За sqlite: използва се db_path. За останалите: db_host, db_port, db_name, db_user, db_password.

#db_type=sqlite
#db_path=ip_inventory.db

# --- PostgreSQL (libpq) – раскоментирай след инсталиране на libpq-dev и прекомпилиране ---
 db_type=postgresql
 db_host=test.greenhamr.org
 db_port=5432
 db_name=viva
 db_user=billing_bs
 db_password=

# --- MSSQL (ODBC) – раскоментирай за използване ---
# db_type=mssql
# db_connection=odbc
# db_connection_string=Driver={ODBC Driver 17 for SQL Server};Server=localhost,1433;Database=ipinv;Uid=user;Pwd=pass;

# --- Oracle (ODBC) – раскоментирай за използване ---
# db_type=oracle
# db_connection=odbc
# db_connection_string=Driver={Oracle in instantclient};Dbq=localhost:1521/ORCL;Uid=user;Pwd=pass;
