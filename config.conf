# IP Inventory backend – configuration
# Environment variables (IPINVENTORY_*) override values from this file.

host=127.0.0.1
port=8888

# --- Database ---
# db_type: sqlite | postgresql | mssql | oracle
# For sqlite use db_path. For others: db_host, db_port, db_name, db_user, db_password.

#db_type=sqlite
#db_path=ip_inventory.db

# --- PostgreSQL (libpq) – uncomment after installing libpq-dev and rebuilding ---
 db_type=postgresql
 db_host=test.greenhamr.org
 db_port=5432
 db_name=viva
 db_user=billing_bs
 db_password=

# --- MSSQL (ODBC) – uncomment to use ---
# db_type=mssql
# db_connection=odbc
# db_connection_string=Driver={ODBC Driver 17 for SQL Server};Server=localhost,1433;Database=ipinv;Uid=user;Pwd=pass;

# --- Oracle (ODBC) – uncomment to use ---
# db_type=oracle
# db_connection=odbc
# db_connection_string=Driver={Oracle in instantclient};Dbq=localhost:1521/ORCL;Uid=user;Pwd=pass;
